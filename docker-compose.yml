services:
  iris:
    build: .
    image: iris:latest
    container_name: iris
    ports:
      - "5000:5000"
    volumes:
      - iris_db:/app/instance
      - iris_uploads:/app/static/uploads
    restart: unless-stopped

volumes:
  iris_db:
  iris_uploads:
